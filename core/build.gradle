//添加依赖文件
dependencies {
    compile(
            libraries."spring-core",
            libraries."spring-context"
    )
    testCompile(
            group: 'junit', name: 'junit', version: '4.11'
    )
}